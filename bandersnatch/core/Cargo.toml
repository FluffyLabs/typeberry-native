[package]
name = "bandersnatch-core"
version = "0.5.1"
edition = "2024"

[dependencies]
ark-vrf = { version = "0.2.2", features = [
    "bandersnatch",
    "ring",
] }
hex = "0.4.3"
getrandom = { version = "0.2", features = ["js"] }

[dev-dependencies]
hex = "0.4.3"

[features]
default = []
